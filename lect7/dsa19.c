#include <stdio.h>
void main()
{
    int num, len;
    printf("Enter A no. ");
    scanf("%d", &num);
    printf("The prime numbers from %d to %d are : ", 0, num);
    for (int i = num / 2; i > 1; i--)
    {
    }
}