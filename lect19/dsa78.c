#include <stdio.h>
void main()
{
    char str[20];
    scanf("%s", str);

    int len = 0;
    for (int i = 0; str[i] != '\0'; i++)
    {
        len++;
    }
    int i = 0;
    for (int key = str[i]; i < len;i++){
        
    }
}