#include <stdio.h>
void main()
{
    int num;

    printf("Enter a No.");
    scanf("%d", &num);
    if(num%2==0)
    {
        printf("The No. is Even");
    }
    else{
        printf("The No. is Even");
    }
}
